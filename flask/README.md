# DSA

## Packages used are

* flask
* flask-sqlalchemy
* flask-migrate
* python-dotenv

## DB migrations

* init DB: `flask db init`
* generate migration: `flask db migrate -m "msg"`
* apply migration: `flask db upgrade`
